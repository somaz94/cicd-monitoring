## Application controller
controller:
  replicas: 1
  env:
    # QPS/burst raised from 50/100 so large syncs are not client-throttled.
    - name: ARGOCD_K8S_CLIENT_QPS
      value: "150"
    - name: ARGOCD_K8S_CLIENT_BURST
      value: "300"
    # Enable pprof endpoint on :6060 for goroutine leak debugging.
    # Access: kubectl port-forward -n argocd argocd-application-controller-0 6060:6060
    #        curl localhost:6060/debug/pprof/goroutine?debug=2 > goroutine.txt
    # See: cicd/argo-cd/docs/ghost-alarm-incident-2026-04-23.md - post-incident follow-up section.
    - name: ARGOCD_APPLICATION_CONTROLLER_PPROF
      value: "true"
  resources:
    limits:
      cpu: 2000m
      memory: 6Gi
    requests:
      cpu: 1000m
      memory: 3Gi
  # ServiceMonitor (Prometheus metrics)
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      namespace: monitoring
      interval: 30s
      additionalLabels:
        release: kube-prometheus-stack

## Dex
dex:
  # -- Enable dex
  enabled: true

## Server
server:
  replicas: 1
  # Argo CD server ingress configuration
  # Disabled after Phase 6 cutover (2026-04-17) — NGF owns .55. HTTPRoute(below) is sole path.
  ingress:
    # -- Enable an ingress resource for the Argo CD server
    enabled: false
    ## Ref: https://argo-cd.readthedocs.io/en/stable/operator-manual/ingress/#option-1-ssl-passthrough
    annotations:
      nginx.ingress.kubernetes.io/force-ssl-redirect: "false"   # Disable SSL redirection
      nginx.ingress.kubernetes.io/ssl-passthrough: "false"      # Disable SSL passthrough

    # -- Defines which ingress controller will implement the resource
    ingressClassName: "nginx"

    # -- The path to Argo CD server
    path: /

    # -- Ingress path type. One of `Exact`, `Prefix` or `ImplementationSpecific`
    pathType: Prefix

  # sectionName: https only — the HTTP listener belongs to the argocd-https-redirect route (dev.yaml extraObjects).
  httproute:
    enabled: true
    parentRefs:
      - name: ngf
        namespace: nginx-gateway
        sectionName: https
    hostnames:
      - argocd.example.com
    rules:
      - matches:
          - path:
              type: PathPrefix
              value: /
  # ServiceMonitor (Prometheus metrics)
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      namespace: monitoring
      interval: 30s
      additionalLabels:
        release: kube-prometheus-stack

## Repo Server
repoServer:
  replicas: 1
  env:
    # Raised from 90s: large Helm charts overrun manifest generation.
    - name: ARGOCD_EXEC_TIMEOUT
      value: "180"
    - name: ARGOCD_GIT_ATTEMPTS_COUNT
      value: "3"
    # Warning: verify no interdependencies between templates.
    - name: ARGOCD_HELM_ALLOW_CONCURRENCY
      value: "true"
  resources:
    limits:
      cpu: 1000m
      memory: 2Gi
    requests:
      cpu: 500m
      memory: 1Gi
  # ServiceMonitor (Prometheus metrics)
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      namespace: monitoring
      interval: 30s
      additionalLabels:
        release: kube-prometheus-stack

## ApplicationSet controller
applicationSet:
  replicas: 1
