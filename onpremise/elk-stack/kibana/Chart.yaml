apiVersion: v2
name: kibana
description: Kibana Custom Resource wrapped as a local Helm chart for ECK
type: application
version: 0.1.0
# appVersion tracks the Kibana image version rendered by the CR.
# Keep in sync with values/mgmt.yaml: .version
appVersion: "9.0.0"
